import React from 'react'

import AgriChat from '../AgriChat'


function Earnings({data,title,units,normUnit}) {
    return <AgriChat data={data} title={title} units={units}/>
}

export default Earnings
